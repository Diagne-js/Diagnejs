import {reUse, create, set} from 'diagne'

export const GetStarted = () => {
  
  const links = create([1,3,45,33,76,55])
  
  return `
     <section class="getStarted">
         learn diagne.js at <a href="">diagne.js website</a>
      </section>
   
  `
}
